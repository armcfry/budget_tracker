info:
  name: GET-Accounts
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}:{{port}}/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
