info:
  name: DELETE-Account
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{host}}:{{port}}/accounts/{{account_id}}"
  auth: inherit

runtime:
  variables:
    - name: account_id
      value: "4"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
