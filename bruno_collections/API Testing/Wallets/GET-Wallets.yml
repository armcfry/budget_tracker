info:
  name: GET-Wallets
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}:{{port}}/wallets"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
