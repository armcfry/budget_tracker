# budget_tracker
Web application to replace google sheets as a budget tracking solution

## Running the App
Ensure that docker is running and then run the following commands.
1. `source set_env.sh` (sets env. vars that docker compose expects)
2. `make up` (spins up the docker containers for the db, frontend, backend)
